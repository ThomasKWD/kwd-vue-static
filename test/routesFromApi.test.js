import generateNamedPath from '../modules/routesFromApi'
import test from 'ava'

export default function() {

	test('make routes function returns valid routes to "Referenzen"', (t) => {
//		t.true(typeof generatedRoutes === 'array')
	})
}
